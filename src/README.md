# Source layout reservation

Application code is intentionally not scaffolded until the Phase 0 toolchain and transport gate are ready.

Planned components:

- `A0CameraStitcher.App`
- `A0CameraStitcher.Core`
- `A0CameraStitcher.CameraAgent`
- `A0CameraStitcher.StitchEngine`
- `A0CameraStitcher.Storage`
